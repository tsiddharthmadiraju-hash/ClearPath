{
  "name": "clearpath-backend",
  "version": "1.0.0",
  "description": "ClearPath backend — Express API that turns confusing legal/government documents into plain-language explanations, action checklists, local resources, and a confidence indicator via Claude.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test:analyze": "node scripts/test-analyze.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "pdf-parse": "^1.1.1"
  },
  "optionalDependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
